apiVersion: v2
name: mock-app
description: Mock consumer chart used to render and assert the tpl-library library templates.
type: application
version: 0.1.0
appVersion: "1.4.2"
dependencies:
  - name: tpl-library
    version: "1.3.0"
    repository: "file://.."
