apiVersion: v2
name: mock-app
description: Mock consumer chart used to render and assert the tpllib library templates.
type: application
version: 0.1.0
appVersion: "1.4.2"
dependencies:
  - name: tpllib
    version: "1.0.0"
    repository: "file://.."
